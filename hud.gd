extends Control


export onready var clock = $TimerLayer/MarginContainer/ClockBackground/Clock
export onready var clock_icon = $TimerLayer/MarginContainer/ClockBackground/ClockIcon

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

func _process(delta):
	pass
	

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
